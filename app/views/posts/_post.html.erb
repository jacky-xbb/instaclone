<div class="flex flex-col border max-w-md mx-auto border-gray-200 rounded-md shadow-md space-y-4">
  <div class="flex items-center m-4">
    <%= image_tag(sprintf("avatars/%02d.png", rand(1..27)), alt: 'avatar', class: 'h-12 w-12') %>
    <div class="ml-2 font-semibold">
      <%= post.user.username %>
    </div>
  </div>

  <div class="text-center text-xl">
    <%= post.body %>
  </div>

  <div class="flex justify-between m-4">
    <%= render "posts/like", post: post %>

    <%= turbo_frame_tag dom_id(post, :comments_count) do %>
      <%= pluralize(post.comments.count, "comment") %>
    <% end %>
  </div>

  <div class="m-4 space-x-4">
    <%= render "comments/form", post: post %>
  </div>

  <div class="m-5 pb-4">
    <%= render "comments/list", post: post %>
  </div>
</div>
